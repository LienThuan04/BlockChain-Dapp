<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Order History - laptopshop</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <meta content="" name="keywords">
    <meta content="" name="description">
    <link rel="icon" type="image/svg+xml" href="/im/Logo-Client.svg">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600&family=Raleway:wght@600;800&display=swap"
        rel="stylesheet">

    <!-- Icon Font Stylesheet -->
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.4/css/all.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="/client/lib/lightbox/css/lightbox.min.css" rel="stylesheet">
    <link href="/client/lib/owlcarousel/assets/owl.carousel.min.css" rel="stylesheet">


    <!-- Customized Bootstrap Stylesheet -->
    <link href="/client/css/bootstrap.min.css" rel="stylesheet">

    <!-- Template Stylesheet -->
    <link href="/client/css/style.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/jquery-toast-plugin/1.3.2/jquery.toast.min.css" integrity="sha512-wJgJNTBBkLit7ymC6vvzM1EcSWeM9mmOu+1USHaRBbHkm6W9EgM0HY27+UtUaprntaYQJF75rc8gjxllKs5OIQ==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body>

    <!-- Spinner Start -->
    <div id="spinner"
        class="show w-100 vh-100 bg-white position-fixed translate-middle top-50 start-50  d-flex align-items-center justify-content-center">
        <div class="spinner-grow text-primary" role="status"></div>
    </div>
    <!-- Spinner End -->


    <!-- Navbar start -->
    <%- include('../layout/header.ejs') -%>
        <!-- Navbar End -->
        <div class="py-4"></div>
        <!-- Single Product Start -->
        <div class="container-fluid py-5 mt-5">
            <div class="container py-5">
                <ol class="breadcrumb mb-0" style="cursor: pointer;">
                                <li class="breadcrumb-item"><a href="/">Home</a></li>
                                <li class="breadcrumb-item active">Lịch sử đơn hàng</li>
                            </ol>
                <br>
                <h1 class="mb-4">Lịch sử đơn hàng</h1>
                <ul class="nav nav-tabs mb-3" id="orderStatusTab" role="tablist">
                    <li class="nav-item" role="presentation">
                        <button class="nav-link active" id="all-tab" data-bs-toggle="tab" data-bs-target="#all"
                            type="button" role="tab" aria-controls="all" aria-selected="true">Tất cả </button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="pending-tab" data-bs-toggle="tab" data-bs-target="#pending"
                            type="button" role="tab" aria-controls="pending" aria-selected="false">Đang chờ xử lý</button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="processing-tab" data-bs-toggle="tab" data-bs-target="#processing"
                            type="button" role="tab" aria-controls="processing"
                            aria-selected="false">Đang xử lý</button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="confirmed-tab" data-bs-toggle="tab" data-bs-target="#confirmed"
                            type="button" role="tab" aria-controls="confirmed" aria-selected="false">Đã xác nhận</button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="packed-tab" data-bs-toggle="tab" data-bs-target="#packed"
                            type="button" role="tab" aria-controls="packed" aria-selected="false">Đã đóng gói</button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="shipped-tab" data-bs-toggle="tab" data-bs-target="#shipped"
                            type="button" role="tab" aria-controls="shipped" aria-selected="false">Đang giao</button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="delivered-tab" data-bs-toggle="tab" data-bs-target="#delivered"
                            type="button" role="tab" aria-controls="delivered" aria-selected="false">Đã giao</button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="cancelled-tab" data-bs-toggle="tab" data-bs-target="#cancelled"
                            type="button" role="tab" aria-controls="cancelled" aria-selected="false">Đã hủy</button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="returned-tab" data-bs-toggle="tab" data-bs-target="#returned"
                            type="button" role="tab" aria-controls="returned" aria-selected="false">Đã trả hàng</button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="refunded-tab" data-bs-toggle="tab" data-bs-target="#refunded"
                            type="button" role="tab" aria-controls="refunded" aria-selected="false">Đã hoàn tiền</button>
                    </li>
                </ul>
                <div class="tab-content mb-5" id="orderStatusTabContent">
                    <div class="tab-pane fade show active" id="all" role="tabpanel" aria-labelledby="all-tab">
                        <% if (orderHistory && orderHistory.length> 0) { %>
                            <% orderHistory.forEach((order) => { %>
                            <!-- Dữ liệu đơn hàng sẽ được render tại đây bằng EJS, mỗi đơn là một card -->
                            <div class="row g-3">
                                <div class="col-12">
                                    <div class="card shadow-sm mb-3 
                                        <% if(order.statusOrder==='PENDING'){ %>border-warning<% } %>
                                        <% if(order.statusOrder==='PROCESSING'){ %>border-info<% } %>
                                        <% if(order.statusOrder==='CONFIRMED'){ %>border-primary<% } %>
                                        <% if(order.statusOrder==='PACKED'){ %>border-secondary<% } %>
                                        <% if(order.statusOrder==='SHIPPED'){ %>border-info<% } %>
                                        <% if(order.statusOrder==='DELIVERED'){ %>border-success<% } %>
                                        <% if(order.statusOrder==='CANCELLED'){ %>border-danger<% } %>
                                        <% if(order.statusOrder==='RETURNED'){ %>border-warning<% } %>
                                        <% if(order.statusOrder==='REFUNDED'){ %>border-warning<% } %>
                                    ">
                                        <div
                                            class="card-body d-flex flex-column align-items-center justify-content-between">
                                            <div class="w-100">
                                                <div
                                                    class="d-flex flex-wrap align-items-center justify-content-between ">
                                                    <h5 class="card-title mb-1">Mã đơn hàng:
                                                        <span class="text-primary"><%= order.id %>
                                                        </span>
                                                    </h5>
                                                    <p class="mb-1">Trạng thái: <span
                                                            class="fw-bold text-success">
                                                            <% if (order.statusOrder === 'PENDING') { %>
                                                                Đang chờ xử lý 
                                                            <% } else if (order.statusOrder === 'PROCESSING') { %>
                                                                Đang xử lý
                                                            <% } else if (order.statusOrder === 'CONFIRMED') { %>
                                                                Đã xác nhận
                                                            <% } else if (order.statusOrder === 'PACKED') { %>
                                                                Đã đóng gói
                                                            <% } else if (order.statusOrder === 'SHIPPED') { %>
                                                                Đã giao cho đơn vị vận chuyển
                                                            <% } else if (order.statusOrder === 'DELIVERED') { %>
                                                                Đã giao thành công
                                                            <% } else if (order.statusOrder === 'CANCELLED') { %>
                                                                Đã hủy
                                                            <% } else if (order.statusOrder === 'RETURNED') { %>
                                                                Đã trả hàng
                                                            <% } else if (order.statusOrder === 'REFUNDED') { %>
                                                                Đã hoàn tiền
                                                            <% } else { %>
                                                                Không xác định
                                                            <% } %>
                                                        </span>
                                                    </p>
                                                </div>
                                                <br>
                                                <div class="table-responsive">
                                                    <table class="table text-center">
                                                        <thead>
                                                            <tr>
                                                                <th scope="col">Sản phẩm</th>
                                                                <th scope="col">Tên</th>
                                                                <th scope="col">Cấu hình</th>
                                                                <th scope="col">Giá</th>
                                                                <th scope="col">Số lượng</th>
                                                                <th scope="col">Tổng cộng</th>
                                                            </tr>
                                                        </thead>
                                                        <tbody>
                                                            <% if ( order.orderDetails && order.orderDetails.length > 0) { %>
                                                                <% order.orderDetails.forEach((item, index)=> { %>
                                                                    <tr class="detailCartId:<%= item.id %>">
                                                                        <th scope="row">
                                                                            <div class="d-flex align-items-center justify-content-center">
                                                                                <img src="<%= item.product.image ? '/images/products/' + item.product.image : '/client/img/default-product.png' %>"
                                                                                    class="img-fluid me-5 rounded-circle"
                                                                                    style="width: 80px; height: 80px; min-width: 50px; min-height: 50px;"
                                                                                    alt="image product <%= item.product.name %>">
                                                                            </div>
                                                                        </th>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-primary">
                                                                                <a href="/product/<%= item.product.id %>"
                                                                                    class="text-decoration-none fw-bold">
                                                                                    <%= item.product.name %>
                                                                                </a>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <% if (item.productVariant && item.productVariant.id) { %>
                                                                                <div class="d-flex justify-content-center align-items-center mt-4 w-100">
                                                                                    <span class="badge bg-info text-dark fs-5 px-4 py-2" style="font-size: 0.8rem !important;">
                                                                                        <%= item.productVariant.cpu %> | <%= item.productVariant.memory %> | <%= item.productVariant.color %>
                                                                                    </span>
                                                                                </div>
                                                                            <% } else { %>
                                                                                <span class="text-muted">Không có</span>
                                                                            <% } %>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-dark fw-bold">
                                                                                <% const variantPriceMore = (item.productVariant && item.productVariant.priceMore) ? item.productVariant.priceMore : 0; %>
                                                                                <% const unitPrice = (item.product && item.product.price ? item.product.price : 0) + variantPriceMore; %>
                                                                                <%= unitPrice.toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-warning fw-bold">
                                                                                <%= item.quantity %>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-danger fw-bold" data-OutPutCartDetail-id="<%= item.id %>">
                                                                                <%= (unitPrice * item.quantity).toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof item.lineCrypto !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                                                                                <div class="text-muted small">~ <%= item.lineCrypto %> <%= cryptoActive.symbol || cryptoActive.code %></div>
                                                                            <% } %>
                                                                        </td>
                                                                    </tr>
                                                                    <% }); %>
                                                                        <% } else { %>
                                                                            <tr>
                                                                                <td colspan="6" class="text-center">Giỏ
                                                                                    hàng trống</td>
                                                                            </tr>
                                                                            <% } %>
                                                        </tbody>
                                                    </table>
                                                </div>
                                                <div class="d-flex flex-wrap align-items-center justify-content-end g-3"
                                                    style="gap: 1rem;">
                                                    <% const _pm = String(order.paymentMethod || '').toUpperCase(); const _ps = String(order.paymentStatus || '').toUpperCase(); const _isPaid = (_pm === 'PAYPAL' || _pm === 'CRYPTO') && (_ps === 'PAID' || _ps === 'PAYMENT_PAID'); %>
                                                    <p class="mb-1 mx-2">Phương thức thanh toán: <span
                                                            class="fw-bold text-success"><%= order.paymentMethod %>
                                                        </span>
                                                        <% if (_isPaid) { %>
                                                            <span class="badge bg-success ms-2">Đã thanh toán</span>
                                                        <% } %>
                                                    </p>
                                                    <p class="mb-1 mx-2">Số lượng sản phẩm: <span
                                                            class="fw-bold text-warning"><%= order.orderDetails.map(item => item.quantity).reduce((a, b) => a + b, 0) %>
                                                        </span></p>
                            <p class="mb-1 mx-4">Tổng số tiền: <span
                                class="fw-bold text-danger"><%= order.totalPrice.toLocaleString('vi-VN') %> VND</span></p>
                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof order.cryptoTotal !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                                <p class="mb-1 mx-4 text-muted small">Crypto total: ~ <%= order.cryptoTotal %> <%= cryptoActive.symbol || cryptoActive.code %></p>
                            <% } %>
                                                </div>
                                            </div>
                                            <% if (order.statusOrder === 'DELIVERED') { %>
                                                <% if (checkReviewed && checkReviewed.some(item => item.orderId === order.id)) { %>                                                        
                                                    <div class="alert alert-success w-100 text-center mb-0" role="alert">
                                                        Đơn hàng đã được giao thành công. Cảm ơn bạn đã mua sắm tại LaptopShop!
                                                    </div>
                                                <% } else { %>
                                                    <div class="alert alert-warning w-100 text-center mb-0" role="alert">
                                                        Đơn hàng này đã được giao thành công tới bạn. Vui lòng hoàn tất đánh giá để nhận ưu đãi từ LaptopShop!
                                                    </div>
                                                <% } %>
                                            <% } %>
                                            <div class="mt-3 d-flex justify-content-end w-100" style="gap: 0.5rem;">
                                                <% if (order.statusOrder === 'DELIVERED') { %>
                                                    <% if (checkReviewed && checkReviewed.some(item => item.orderId === order.id)) { %>
                                                        <a href="/review/<%= order.id %>" class="btn btn-outline-success btn-sm">Xem đánh giá</a>
                                                    <% } else { %>
                                                        <a href="/review/<%= order.id %>" class="btn btn-outline-success btn-sm">Đánh giá sản phẩm</a>
                                                    <% } %>
                                                <% } %>
                                                <a href="/order-history/<%= order.id %>" class="btn btn-outline-primary btn-sm">Xem chi tiết</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <% }) %>
                        <% } else { %>
                                <div class="alert alert-info">Bạn không có đơn hàng nào.</div>
                        <% } %>
                    </div>
                    <div class="tab-pane fade" id="pending" role="tabpanel" aria-labelledby="pending-tab">
                        <% if (orderHistory && orderHistory.length> 0 && orderHistory.some(order => order.statusOrder === 'PENDING')) { %>
                            <% orderHistory.forEach((order) => { %>
                            <!-- Dữ liệu đơn hàng sẽ được render tại đây bằng EJS, mỗi đơn là một card -->
                             <% if (order.statusOrder === 'PENDING') { %>
                            <div class="row g-3">
                                <div class="col-12">
                                    <div class="card shadow-sm mb-3 border-warning">
                                        <div
                                            class="card-body d-flex flex-column align-items-center justify-content-between">
                                            <div class="w-100">
                                                <div
                                                    class="d-flex flex-wrap align-items-center justify-content-between ">
                                                    <h5 class="card-title mb-1">Mã đơn hàng:
                                                        <span class="text-primary"><%= order.id %>
                                                        </span>
                                                    </h5>
                                                    <p class="mb-1">Trạng thái: <span
                                                            class="fw-bold text-success">
                                                            <% if (order.statusOrder === 'PENDING') { %>
                                                                Đang chờ xử lý 
                                                            <% } else if (order.statusOrder === 'PROCESSING') { %>
                                                                Đang xử lý
                                                            <% } else if (order.statusOrder === 'CONFIRMED') { %>
                                                                Đã xác nhận
                                                            <% } else if (order.statusOrder === 'PACKED') { %>
                                                                Đã đóng gói
                                                            <% } else if (order.statusOrder === 'SHIPPED') { %>
                                                                Đã giao cho đơn vị vận chuyển
                                                            <% } else if (order.statusOrder === 'DELIVERED') { %>
                                                                Đã giao thành công
                                                            <% } else if (order.statusOrder === 'CANCELLED') { %>
                                                                Đã hủy
                                                            <% } else if (order.statusOrder === 'RETURNED') { %>
                                                                Đã trả hàng
                                                            <% } else if (order.statusOrder === 'REFUNDED') { %>
                                                                Đã hoàn tiền
                                                            <% } else { %>
                                                                Không xác định
                                                            <% } %>
                                                        </span>
                                                    </p>
                                                </div>
                                                <br>
                                                <div class="table-responsive">
                                                    <table class="table text-center">
                                                        <thead>
                                                            <tr>
                                                                <th scope="col">Sản phẩm</th>
                                                                <th scope="col">Tên</th>
                                                                <th scope="col">Cấu hình</th>
                                                                <th scope="col">Giá</th>
                                                                <th scope="col">Số lượng</th>
                                                                <th scope="col">Tổng cộng</th>
                                                            </tr>
                                                        </thead>
                                                        <tbody>
                                                            <% if ( order.orderDetails && order.orderDetails.length > 0) { %>
                                                                <% order.orderDetails.forEach((item, index)=> { %>
                                                                    <tr class="detailCartId:<%= item.id %>">
                                                                        <th scope="row">
                                                                            <div
                                                                                class="d-flex align-items-center justify-content-center">
                                                                                <img src="<%= item.product.image ? '/images/products/' + item.product.image : '/client/img/default-product.png' %>"
                                                                                    class="img-fluid me-5 rounded-circle"
                                                                                    style="width: 80px; height: 80px; min-width: 50px; min-height: 50px;"
                                                                                    alt="image product <%= item.product.name %>">
                                                                            </div>
                                                                        </th>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-primary">
                                                                                <a href="/product/<%= item.product.id %>"
                                                                                    class="text-decoration-none fw-bold">
                                                                                    <%= item.product.name %>
                                                                                </a>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <% if (item.productVariant && item.productVariant.id) { %>
                                                                                <div class="d-flex justify-content-center align-items-center mt-4 w-100">
                                                                                    <span class="badge bg-info text-dark fs-5 px-4 py-2" style="font-size: 0.8rem !important;">
                                                                                        <%= item.productVariant.cpu %> | <%= item.productVariant.memory %> | <%= item.productVariant.color %>
                                                                                    </span>
                                                                                </div>
                                                                            <% } else { %>
                                                                                <span class="text-muted">Không có</span>
                                                                            <% } %>
                                                                        </td>
                                                                        <td>
                                                                            <% const variantPriceMore = (item.productVariant && item.productVariant.priceMore) ? item.productVariant.priceMore : 0; %>
                                                                            <% const unitPrice = (item.product && item.product.price ? item.product.price : 0) + variantPriceMore; %>
                                                                            <p class="mb-0 mt-4 text-dark fw-bold">
                                                                                <%= unitPrice.toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-warning fw-bold">
                                                                                <%= item.quantity %>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-danger fw-bold"
                                                                                data-OutPutCartDetail-id="<%= item.id %>">
                                                                                <%= (unitPrice * item.quantity).toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof item.lineCrypto !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                                                                                <div class="text-muted small">~ <%= item.lineCrypto %> <%= cryptoActive.symbol || cryptoActive.code %></div>
                                                                            <% } %>
                                                                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof item.lineCrypto !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                                                                                <div class="text-muted small">~ <%= item.lineCrypto %> <%= cryptoActive.symbol || cryptoActive.code %></div>
                                                                            <% } %>
                                                                        </td>
                                                                    </tr>
                                                                    <% }); %>
                                                                        <% } else { %>
                                                                            <tr>
                                                                                <td colspan="6" class="text-center">Giỏ
                                                                                    hàng trống</td>
                                                                            </tr>
                                                                            <% } %>
                                                        </tbody>
                                                    </table>
                                                </div>
                                                <div class="d-flex flex-wrap align-items-center justify-content-end g-3"
                                                    style="gap: 1rem;">
                                                    <% const _pm = String(order.paymentMethod || '').toUpperCase(); const _ps = String(order.paymentStatus || '').toUpperCase(); const _isPaid = (_pm === 'PAYPAL' || _pm === 'CRYPTO') && (_ps === 'PAID' || _ps === 'PAYMENT_PAID'); %>
                                                    <p class="mb-1 mx-2">Phương thức thanh toán: <span
                                                            class="fw-bold text-success"><%= order.paymentMethod %>
                                                        </span>
                                                        <% if (_isPaid) { %>
                                                            <span class="badge bg-success ms-2">Đã thanh toán</span>
                                                        <% } %>
                                                    </p>
                                                    <p class="mb-1 mx-2">Số lượng sản phẩm: <span
                                                            class="fw-bold text-warning"><%= order.orderDetails.map(item => item.quantity).reduce((a, b) => a + b, 0) %>
                                                        </span></p>
                            <p class="mb-1 mx-4">Tổng số tiền: <span
                                class="fw-bold text-danger"><%= order.totalPrice.toLocaleString('vi-VN') %> VND</span></p>
                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof order.cryptoTotal !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                            <p class="mb-1 mx-4 text-muted small">Crypto total: ~ <%= order.cryptoTotal %> <%= cryptoActive.symbol || cryptoActive.code %></p>
                            <% } %>
                                                </div>
                                            </div>
                                            <div class="mt-3 d-flex justify-content-end w-100">
                                                <a href="/order-history/<%= order.id %>" class="btn btn-outline-primary btn-sm">Xem chi tiết</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <% } %>
                            <% }) %>
                        <% } else { %>
                                <div class="alert alert-info">Bạn không có đơn hàng nào ở trạng thái <a style="text-decoration: underline;">đang chờ xử lý</a>.</div>
                        <% } %>
                    </div>
                    <div class="tab-pane fade" id="processing" role="tabpanel" aria-labelledby="processing-tab">
                        <% if (orderHistory && orderHistory.length> 0 && orderHistory.some(order => order.statusOrder === 'PROCESSING')) { %>
                            <% orderHistory.forEach((order) => { %>
                                <% if (order.statusOrder === 'PROCESSING') { %>
                            <!-- Dữ liệu đơn hàng sẽ được render tại đây bằng EJS, mỗi đơn là một card -->
                            <div class="row g-3">
                                <div class="col-12">
                                    <div class="card shadow-sm mb-3 border-info">
                                        <div
                                            class="card-body d-flex flex-column align-items-center justify-content-between">
                                            <div class="w-100">
                                                <div
                                                    class="d-flex flex-wrap align-items-center justify-content-between ">
                                                    <h5 class="card-title mb-1">Mã đơn hàng:
                                                        <span class="text-primary"><%= order.id %>
                                                        </span>
                                                    </h5>
                                                    <p class="mb-1">Trạng thái: <span
                                                            class="fw-bold text-success">
                                                            <% if (order.statusOrder === 'PENDING') { %>
                                                                Đang chờ xử lý 
                                                            <% } else if (order.statusOrder === 'PROCESSING') { %>
                                                                Đang xử lý
                                                            <% } else if (order.statusOrder === 'CONFIRMED') { %>
                                                                Đã xác nhận
                                                            <% } else if (order.statusOrder === 'PACKED') { %>
                                                                Đã đóng gói
                                                            <% } else if (order.statusOrder === 'SHIPPED') { %>
                                                                Đã giao cho đơn vị vận chuyển
                                                            <% } else if (order.statusOrder === 'DELIVERED') { %>
                                                                Đã giao thành công
                                                            <% } else if (order.statusOrder === 'CANCELLED') { %>
                                                                Đã hủy
                                                            <% } else if (order.statusOrder === 'RETURNED') { %>
                                                                Đã trả hàng
                                                            <% } else if (order.statusOrder === 'REFUNDED') { %>
                                                                Đã hoàn tiền
                                                            <% } else { %>
                                                                Không xác định
                                                            <% } %>
                                                        </span>
                                                    </p>
                                                </div>
                                                <br>
                                                <div class="table-responsive">
                                                    <table class="table text-center">
                                                        <thead>
                                                            <tr>
                                                                <th scope="col">Sản phẩm</th>
                                                                <th scope="col">Tên</th>
                                                                <th scope="col">Cấu hình</th>
                                                                <th scope="col">Giá</th>
                                                                <th scope="col">Số lượng</th>
                                                                <th scope="col">Tổng cộng</th>
                                                            </tr>
                                                        </thead>
                                                        <tbody>
                                                            <% if ( order.orderDetails && order.orderDetails.length > 0) { %>
                                                                <% order.orderDetails.forEach((item, index)=> { %>
                                                                    <tr class="detailCartId:<%= item.id %>">
                                                                        <th scope="row">
                                                                            <div
                                                                                class="d-flex align-items-center justify-content-center">
                                                                                <img src="<%= item.product.image ? '/images/products/' + item.product.image : '/client/img/default-product.png' %>"
                                                                                    class="img-fluid me-5 rounded-circle"
                                                                                    style="width: 80px; height: 80px; min-width: 50px; min-height: 50px;"
                                                                                    alt="image product <%= item.product.name %>">
                                                                            </div>
                                                                        </th>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-primary">
                                                                                <a href="/product/<%= item.product.id %>"
                                                                                    class="text-decoration-none fw-bold">
                                                                                    <%= item.product.name %>
                                                                                </a>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <% if (item.productVariant && item.productVariant.id) { %>
                                                                                <div class="d-flex justify-content-center align-items-center mt-4 w-100">
                                                                                    <span class="badge bg-info text-dark fs-5 px-4 py-2" style="font-size: 0.8rem !important;">
                                                                                        <%= item.productVariant.cpu %> | <%= item.productVariant.memory %> | <%= item.productVariant.color %>
                                                                                    </span>
                                                                                </div>
                                                                            <% } else { %>
                                                                                <span class="text-muted">Không có</span>
                                                                            <% } %>
                                                                        </td>
                                                                        <td>
                                                                            <% const variantPriceMore = (item.productVariant && item.productVariant.priceMore) ? item.productVariant.priceMore : 0; %>
                                                                            <% const unitPrice = (item.product && item.product.price ? item.product.price : 0) + variantPriceMore; %>
                                                                            <p class="mb-0 mt-4 text-dark fw-bold">
                                                                                <%= unitPrice.toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-warning fw-bold">
                                                                                <%= item.quantity %>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-danger fw-bold"
                                                                                data-OutPutCartDetail-id="<%= item.id %>">
                                                                                <%= (unitPrice * item.quantity).toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof item.lineCrypto !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                                                                                <div class="text-muted small">~ <%= item.lineCrypto %> <%= cryptoActive.symbol || cryptoActive.code %></div>
                                                                            <% } %>
                                                                        </td>
                                                                    </tr>
                                                                    <% }); %>
                                                                        <% } else { %>
                                                                            <tr>
                                                                                <td colspan="6" class="text-center">Giỏ
                                                                                    hàng trống</td>
                                                                            </tr>
                                                                            <% } %>
                                                        </tbody>
                                                    </table>
                                                </div>
                                                <div class="d-flex flex-wrap align-items-center justify-content-end g-3"
                                                    style="gap: 1rem;">
                                                    <% const _pm = String(order.paymentMethod || '').toUpperCase(); const _ps = String(order.paymentStatus || '').toUpperCase(); const _isPaid = (_pm === 'PAYPAL' || _pm === 'CRYPTO') && (_ps === 'PAID' || _ps === 'PAYMENT_PAID'); %>
                                                    <p class="mb-1 mx-2">Phương thức thanh toán: <span
                                                            class="fw-bold text-success"><%= order.paymentMethod %>
                                                        </span>
                                                        <% if (_isPaid) { %>
                                                            <span class="badge bg-success ms-2">Đã thanh toán</span>
                                                        <% } %>
                                                    </p>
                                                    <p class="mb-1 mx-2">Số lượng sản phẩm: <span
                                                            class="fw-bold text-warning"><%= order.orderDetails.map(item => item.quantity).reduce((a, b) => a + b, 0) %>
                                                        </span></p>
                            <p class="mb-1 mx-4">Tổng số tiền: <span
                                class="fw-bold text-danger"><%= order.totalPrice.toLocaleString('vi-VN') %> VND</span></p>
                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof order.cryptoTotal !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                            <p class="mb-1 mx-4 text-muted small">Crypto total: ~ <%= order.cryptoTotal %> <%= cryptoActive.symbol || cryptoActive.code %></p>
                            <% } %>
                                                </div>
                                            </div>
                                            <div class="mt-3 d-flex justify-content-end w-100">
                                                <a href="/order-history/<%= order.id %>" class="btn btn-outline-primary btn-sm">Xem chi tiết</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <% } %>
                            <% }) %>
                        <% } else { %>
                                <div class="alert alert-info">Bạn không có đơn hàng nào ở trạng thái <a style="text-decoration: underline;">đang xử lý</a>.</div>
                        <% } %>
                    </div>
                    <div class="tab-pane fade" id="confirmed" role="tabpanel" aria-labelledby="confirmed-tab">
                        <% if (orderHistory && orderHistory.length> 0 && orderHistory.some(order => order.statusOrder === 'CONFIRMED')) { %>
                            <% orderHistory.forEach((order) => { %>
                                <% if (order.statusOrder === 'CONFIRMED') { %>
                            <!-- Dữ liệu đơn hàng sẽ được render tại đây bằng EJS, mỗi đơn là một card -->
                            <div class="row g-3">
                                <div class="col-12">
                                    <div class="card shadow-sm mb-3 border-primary">
                                        <div
                                            class="card-body d-flex flex-column align-items-center justify-content-between">
                                            <div class="w-100">
                                                <div
                                                    class="d-flex flex-wrap align-items-center justify-content-between ">
                                                    <h5 class="card-title mb-1">Mã đơn hàng:
                                                        <span class="text-primary"><%= order.id %>
                                                        </span>
                                                    </h5>
                                                    <p class="mb-1">Trạng thái: <span
                                                            class="fw-bold text-success">
                                                            <% if (order.statusOrder === 'PENDING') { %>
                                                                Đang chờ xử lý 
                                                            <% } else if (order.statusOrder === 'PROCESSING') { %>
                                                                Đang xử lý
                                                            <% } else if (order.statusOrder === 'CONFIRMED') { %>
                                                                Đã xác nhận
                                                            <% } else if (order.statusOrder === 'PACKED') { %>
                                                                Đã đóng gói
                                                            <% } else if (order.statusOrder === 'SHIPPED') { %>
                                                                Đã giao cho đơn vị vận chuyển
                                                            <% } else if (order.statusOrder === 'DELIVERED') { %>
                                                                Đã giao thành công
                                                            <% } else if (order.statusOrder === 'CANCELLED') { %>
                                                                Đã hủy
                                                            <% } else if (order.statusOrder === 'RETURNED') { %>
                                                                Đã trả hàng
                                                            <% } else if (order.statusOrder === 'REFUNDED') { %>
                                                                Đã hoàn tiền
                                                            <% } else { %>
                                                                Không xác định
                                                            <% } %>
                                                        </span>
                                                    </p>
                                                </div>
                                                <br>
                                                <div class="table-responsive">
                                                    <table class="table text-center">
                                                        <thead>
                                                            <tr>
                                                                <th scope="col">Sản phẩm</th>
                                                                <th scope="col">Tên</th>
                                                                <th scope="col">Cấu hình</th>
                                                                <th scope="col">Giá</th>
                                                                <th scope="col">Số lượng</th>
                                                                <th scope="col">Tổng cộng</th>
                                                            </tr>
                                                        </thead>
                                                        <tbody>
                                                            <% if ( order.orderDetails && order.orderDetails.length > 0) { %>
                                                                <% order.orderDetails.forEach((item, index)=> { %>
                                                                    <tr class="detailCartId:<%= item.id %>">
                                                                        <th scope="row">
                                                                            <div
                                                                                class="d-flex align-items-center justify-content-center">
                                                                                <img src="<%= item.product.image ? '/images/products/' + item.product.image : '/client/img/default-product.png' %>"
                                                                                    class="img-fluid me-5 rounded-circle"
                                                                                    style="width: 80px; height: 80px; min-width: 50px; min-height: 50px;"
                                                                                    alt="image product <%= item.product.name %>">
                                                                            </div>
                                                                        </th>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-primary">
                                                                                <a href="/product/<%= item.product.id %>"
                                                                                    class="text-decoration-none fw-bold">
                                                                                    <%= item.product.name %>
                                                                                </a>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <% if (item.productVariant && item.productVariant.id) { %>
                                                                                <div class="d-flex justify-content-center align-items-center mt-4 w-100">
                                                                                    <span class="badge bg-info text-dark fs-5 px-4 py-2" style="font-size: 0.8rem !important;">
                                                                                        <%= item.productVariant.cpu %> | <%= item.productVariant.memory %> | <%= item.productVariant.color %>
                                                                                    </span>
                                                                                </div>
                                                                            <% } else { %>
                                                                                <span class="text-muted">Không có</span>
                                                                            <% } %>
                                                                        </td>
                                                                        <td>
                                                                            <% const variantPriceMore = (item.productVariant && item.productVariant.priceMore) ? item.productVariant.priceMore : 0; %>
                                                                            <% const unitPrice = (item.product && item.product.price ? item.product.price : 0) + variantPriceMore; %>
                                                                            <p class="mb-0 mt-4 text-dark fw-bold">
                                                                                <%= unitPrice.toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-warning fw-bold">
                                                                                <%= item.quantity %>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-danger fw-bold"
                                                                                data-OutPutCartDetail-id="<%= item.id %>">
                                                                                <%= (unitPrice * item.quantity).toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof item.lineCrypto !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                                                                                <div class="text-muted small">~ <%= item.lineCrypto %> <%= cryptoActive.symbol || cryptoActive.code %></div>
                                                                            <% } %>
                                                                        </td>
                                                                    </tr>
                                                                    <% }); %>
                                                                        <% } else { %>
                                                                            <tr>
                                                                                <td colspan="6" class="text-center">Giỏ
                                                                                    hàng trống</td>
                                                                            </tr>
                                                                            <% } %>
                                                        </tbody>
                                                    </table>
                                                </div>
                                                <div class="d-flex flex-wrap align-items-center justify-content-end g-3"
                                                    style="gap: 1rem;">
                                                    <% const _pm = String(order.paymentMethod || '').toUpperCase(); const _ps = String(order.paymentStatus || '').toUpperCase(); const _isPaid = (_pm === 'PAYPAL' || _pm === 'CRYPTO') && (_ps === 'PAID' || _ps === 'PAYMENT_PAID'); %>
                                                    <p class="mb-1 mx-2">Phương thức thanh toán: <span
                                                            class="fw-bold text-success"><%= order.paymentMethod %>
                                                        </span>
                                                        <% if (_isPaid) { %>
                                                            <span class="badge bg-success ms-2">Đã thanh toán</span>
                                                        <% } %>
                                                    </p>
                                                    <p class="mb-1 mx-2">Số lượng sản phẩm: <span
                                                            class="fw-bold text-warning"><%= order.orderDetails.map(item => item.quantity).reduce((a, b) => a + b, 0) %>
                                                        </span></p>
                            <p class="mb-1 mx-4">Tổng số tiền: <span
                                class="fw-bold text-danger"><%= order.totalPrice.toLocaleString('vi-VN') %> VND</span></p>
                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof order.cryptoTotal !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                            <p class="mb-1 mx-4 text-muted small">Crypto total: ~ <%= order.cryptoTotal %> <%= cryptoActive.symbol || cryptoActive.code %></p>
                            <% } %>
                                                </div>
                                            </div>
                                            <div class="mt-3 d-flex justify-content-end w-100">
                                                <a href="/order-history/<%= order.id %>" class="btn btn-outline-primary btn-sm">Xem chi tiết</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <% } %>
                            <% }) %>
                        <% } else { %>
                                <div class="alert alert-info">Bạn không có đơn hàng nào ở trạng thái <a style="text-decoration: underline;">đã xác nhận</a>.</div>
                        <% } %>
                    </div>
                    <div class="tab-pane fade" id="packed" role="tabpanel" aria-labelledby="packed-tab">
                        <% if (orderHistory && orderHistory.length> 0 && orderHistory.some(order => order.statusOrder === 'PACKED')) { %>
                            <% orderHistory.forEach((order) => { %>
                                <% if (order.statusOrder === 'PACKED') { %>
                            <!-- Dữ liệu đơn hàng sẽ được render tại đây bằng EJS, mỗi đơn là một card -->
                            <div class="row g-3">
                                <div class="col-12">
                                    <div class="card shadow-sm mb-3 border-secondary">
                                        <div
                                            class="card-body d-flex flex-column align-items-center justify-content-between">
                                            <div class="w-100">
                                                <div
                                                    class="d-flex flex-wrap align-items-center justify-content-between ">
                                                    <h5 class="card-title mb-1">Mã đơn hàng:
                                                        <span class="text-primary"><%= order.id %>
                                                        </span>
                                                    </h5>
                                                    <p class="mb-1">Trạng thái: <span
                                                            class="fw-bold text-success">
                                                            <% if (order.statusOrder === 'PENDING') { %>
                                                                Đang chờ xử lý 
                                                            <% } else if (order.statusOrder === 'PROCESSING') { %>
                                                                Đang xử lý
                                                            <% } else if (order.statusOrder === 'CONFIRMED') { %>
                                                                Đã xác nhận
                                                            <% } else if (order.statusOrder === 'PACKED') { %>
                                                                Đã đóng gói
                                                            <% } else if (order.statusOrder === 'SHIPPED') { %>
                                                                Đã giao cho đơn vị vận chuyển
                                                            <% } else if (order.statusOrder === 'DELIVERED') { %>
                                                                Đã giao thành công
                                                            <% } else if (order.statusOrder === 'CANCELLED') { %>
                                                                Đã hủy
                                                            <% } else if (order.statusOrder === 'RETURNED') { %>
                                                                Đã trả hàng
                                                            <% } else if (order.statusOrder === 'REFUNDED') { %>
                                                                Đã hoàn tiền
                                                            <% } else { %>
                                                                Không xác định
                                                            <% } %>
                                                        </span>
                                                    </p>
                                                </div>
                                                <br>
                                                <div class="table-responsive">
                                                    <table class="table text-center">
                                                        <thead>
                                                            <tr>
                                                                <th scope="col">Sản phẩm</th>
                                                                <th scope="col">Tên</th>
                                                                <th scope="col">Cấu hình</th>
                                                                <th scope="col">Giá</th>
                                                                <th scope="col">Số lượng</th>
                                                                <th scope="col">Tổng cộng</th>
                                                            </tr>
                                                        </thead>
                                                        <tbody>
                                                            <% if ( order.orderDetails && order.orderDetails.length > 0) { %>
                                                                <% order.orderDetails.forEach((item, index)=> { %>
                                                                    <tr class="detailCartId:<%= item.id %>">
                                                                        <th scope="row">
                                                                            <div
                                                                                class="d-flex align-items-center justify-content-center">
                                                                                <img src="<%= item.product.image ? '/images/products/' + item.product.image : '/client/img/default-product.png' %>"
                                                                                    class="img-fluid me-5 rounded-circle"
                                                                                    style="width: 80px; height: 80px; min-width: 50px; min-height: 50px;"
                                                                                    alt="image product <%= item.product.name %>">
                                                                            </div>
                                                                        </th>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-primary">
                                                                                <a href="/product/<%= item.product.id %>"
                                                                                    class="text-decoration-none fw-bold">
                                                                                    <%= item.product.name %>
                                                                                </a>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <% if (item.productVariant && item.productVariant.id) { %>
                                                                                <div class="d-flex justify-content-center align-items-center mt-4 w-100">
                                                                                    <span class="badge bg-info text-dark fs-5 px-4 py-2" style="font-size: 0.8rem !important;">
                                                                                        <%= item.productVariant.cpu %> | <%= item.productVariant.memory %> | <%= item.productVariant.color %>
                                                                                    </span>
                                                                                </div>
                                                                            <% } else { %>
                                                                                <span class="text-muted">Không có</span>
                                                                            <% } %>
                                                                        </td>
                                                                        <td>
                                                                            <% const variantPriceMore = (item.productVariant && item.productVariant.priceMore) ? item.productVariant.priceMore : 0; %>
                                                                            <% const unitPrice = (item.product && item.product.price ? item.product.price : 0) + variantPriceMore; %>
                                                                            <p class="mb-0 mt-4 text-dark fw-bold">
                                                                                <%= unitPrice.toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-warning fw-bold">
                                                                                <%= item.quantity %>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-danger fw-bold"
                                                                                data-OutPutCartDetail-id="<%= item.id %>">
                                                                                <%= (unitPrice * item.quantity).toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof item.lineCrypto !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                                                                                <div class="text-muted small">~ <%= item.lineCrypto %> <%= cryptoActive.symbol || cryptoActive.code %></div>
                                                                            <% } %>
                                                                        </td>
                                                                    </tr>
                                                                    <% }); %>
                                                                        <% } else { %>
                                                                            <tr>
                                                                                <td colspan="6" class="text-center">Giỏ
                                                                                    hàng trống</td>
                                                                            </tr>
                                                                            <% } %>
                                                        </tbody>
                                                    </table>
                                                </div>
                                                <div class="d-flex flex-wrap align-items-center justify-content-end g-3"
                                                    style="gap: 1rem;">
                                                    <% const _pm = String(order.paymentMethod || '').toUpperCase(); const _ps = String(order.paymentStatus || '').toUpperCase(); const _isPaid = (_pm === 'PAYPAL' || _pm === 'CRYPTO') && (_ps === 'PAID' || _ps === 'PAYMENT_PAID'); %>
                                                    <p class="mb-1 mx-2">Phương thức thanh toán: <span
                                                            class="fw-bold text-success"><%= order.paymentMethod %>
                                                        </span>
                                                        <% if (_isPaid) { %>
                                                            <span class="badge bg-success ms-2">Đã thanh toán</span>
                                                        <% } %>
                                                    </p>
                                                    <p class="mb-1 mx-2">Số lượng sản phẩm: <span
                                                            class="fw-bold text-warning"><%= order.orderDetails.map(item => item.quantity).reduce((a, b) => a + b, 0) %>
                                                        </span></p>
                            <p class="mb-1 mx-4">Tổng số tiền: <span
                                class="fw-bold text-danger"><%= order.totalPrice.toLocaleString('vi-VN') %> VND</span></p>
                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof order.cryptoTotal !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                            <p class="mb-1 mx-4 text-muted small">Crypto total: ~ <%= order.cryptoTotal %> <%= cryptoActive.symbol || cryptoActive.code %></p>
                            <% } %>
                                                </div>
                                            </div>
                                            <div class="mt-3 d-flex justify-content-end w-100">
                                                <a href="/order-history/<%= order.id %>" class="btn btn-outline-primary btn-sm">Xem chi tiết</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <% } %>
                            <% }) %>
                        <% } else { %>
                                <div class="alert alert-info">Bạn không có đơn hàng nào ở trạng thái <a style="text-decoration: underline;">đã được đóng gói</a>.</div>
                        <% } %>
                    </div>
                    <div class="tab-pane fade" id="shipped" role="tabpanel" aria-labelledby="shipped-tab">
                        <% if (orderHistory && orderHistory.length> 0 && orderHistory.some(order => order.statusOrder === 'SHIPPED')) { %>
                            <% orderHistory.forEach((order) => { %>
                            <!-- Dữ liệu đơn hàng sẽ được render tại đây bằng EJS, mỗi đơn là một card -->
                             <% if (order.statusOrder === 'SHIPPED') { %>
                            <div class="row g-3">
                                <div class="col-12">
                                    <div class="card shadow-sm mb-3 border-info">
                                        <div
                                            class="card-body d-flex flex-column align-items-center justify-content-between">
                                            <div class="w-100">
                                                <div
                                                    class="d-flex flex-wrap align-items-center justify-content-between ">
                                                    <h5 class="card-title mb-1">Mã đơn hàng:
                                                        <span class="text-primary"><%= order.id %>
                                                        </span>
                                                    </h5>
                                                    <p class="mb-1">Trạng thái: <span
                                                            class="fw-bold text-success">
                                                            <% if (order.statusOrder === 'PENDING') { %>
                                                                Đang chờ xử lý 
                                                            <% } else if (order.statusOrder === 'PROCESSING') { %>
                                                                Đang xử lý
                                                            <% } else if (order.statusOrder === 'CONFIRMED') { %>
                                                                Đã xác nhận
                                                            <% } else if (order.statusOrder === 'PACKED') { %>
                                                                Đã đóng gói
                                                            <% } else if (order.statusOrder === 'SHIPPED') { %>
                                                                Đã giao cho đơn vị vận chuyển
                                                            <% } else if (order.statusOrder === 'DELIVERED') { %>
                                                                Đã giao thành công
                                                            <% } else if (order.statusOrder === 'CANCELLED') { %>
                                                                Đã hủy
                                                            <% } else if (order.statusOrder === 'RETURNED') { %>
                                                                Đã trả hàng
                                                            <% } else if (order.statusOrder === 'REFUNDED') { %>
                                                                Đã hoàn tiền
                                                            <% } else { %>
                                                                Không xác định
                                                            <% } %>
                                                        </span>
                                                    </p>
                                                </div>
                                                <br>
                                                <div class="table-responsive">
                                                    <table class="table text-center">
                                                        <thead>
                                                            <tr>
                                                                <th scope="col">Sản phẩm</th>
                                                                <th scope="col">Tên</th>
                                                                <th scope="col">Cấu hình</th>
                                                                <th scope="col">Giá</th>
                                                                <th scope="col">Số lượng</th>
                                                                <th scope="col">Tổng cộng</th>
                                                            </tr>
                                                        </thead>
                                                        <tbody>
                                                            <% if ( order.orderDetails && order.orderDetails.length > 0) { %>
                                                                <% order.orderDetails.forEach((item, index)=> { %>
                                                                    <tr class="detailCartId:<%= item.id %>">
                                                                        <th scope="row">
                                                                            <div
                                                                                class="d-flex align-items-center justify-content-center">
                                                                                <img src="<%= item.product.image ? '/images/products/' + item.product.image : '/client/img/default-product.png' %>"
                                                                                    class="img-fluid me-5 rounded-circle"
                                                                                    style="width: 80px; height: 80px; min-width: 50px; min-height: 50px;"
                                                                                    alt="image product <%= item.product.name %>">
                                                                            </div>
                                                                        </th>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-primary">
                                                                                <a href="/product/<%= item.product.id %>"
                                                                                    class="text-decoration-none fw-bold">
                                                                                    <%= item.product.name %>
                                                                                </a>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <% if (item.productVariant && item.productVariant.id) { %>
                                                                                <div class="d-flex justify-content-center align-items-center mt-4 w-100">
                                                                                    <span class="badge bg-info text-dark fs-5 px-4 py-2" style="font-size: 0.8rem !important;">
                                                                                        <%= item.productVariant.cpu %> | <%= item.productVariant.memory %> | <%= item.productVariant.color %>
                                                                                    </span>
                                                                                </div>
                                                                            <% } else { %>
                                                                                <span class="text-muted">Không có</span>
                                                                            <% } %>
                                                                        </td>
                                                                        <td>
                                                                            <% const variantPriceMore = (item.productVariant && item.productVariant.priceMore) ? item.productVariant.priceMore : 0; %>
                                                                            <% const unitPrice = (item.product && item.product.price ? item.product.price : 0) + variantPriceMore; %>
                                                                            <p class="mb-0 mt-4 text-dark fw-bold">
                                                                                <%= unitPrice.toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-warning fw-bold">
                                                                                <%= item.quantity %>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-danger fw-bold"
                                                                                data-OutPutCartDetail-id="<%= item.id %>">
                                                                                <%= (unitPrice * item.quantity).toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                    </tr>
                                                                    <% }); %>
                                                                        <% } else { %>
                                                                            <tr>
                                                                                <td colspan="6" class="text-center">Giỏ
                                                                                    hàng trống</td>
                                                                            </tr>
                                                                            <% } %>
                                                        </tbody>
                                                    </table>
                                                </div>
                                                <div class="d-flex flex-wrap align-items-center justify-content-end g-3"
                                                    style="gap: 1rem;">
                                                    <% const _pm = String(order.paymentMethod || '').toUpperCase(); const _ps = String(order.paymentStatus || '').toUpperCase(); const _isPaid = (_pm === 'PAYPAL' || _pm === 'CRYPTO') && (_ps === 'PAID' || _ps === 'PAYMENT_PAID'); %>
                                                    <p class="mb-1 mx-2">Phương thức thanh toán: <span
                                                            class="fw-bold text-success"><%= order.paymentMethod %>
                                                        </span>
                                                        <% if (_isPaid) { %>
                                                            <span class="badge bg-success ms-2">Đã thanh toán</span>
                                                        <% } %>
                                                    </p>
                                                    <p class="mb-1 mx-2">Số lượng sản phẩm: <span
                                                            class="fw-bold text-warning"><%= order.orderDetails.map(item => item.quantity).reduce((a, b) => a + b, 0) %>
                                                        </span></p>
                            <p class="mb-1 mx-4">Tổng số tiền: <span
                                class="fw-bold text-danger"><%= order.totalPrice.toLocaleString('vi-VN') %> VND</span></p>
                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof order.cryptoTotal !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                            <p class="mb-1 mx-4 text-muted small">Crypto total: ~ <%= order.cryptoTotal %> <%= cryptoActive.symbol || cryptoActive.code %></p>
                            <% } %>
                                                </div>
                                            </div>
                                            <div class="mt-3 d-flex justify-content-end w-100">
                                                <a href="/order-history/<%= order.id %>" class="btn btn-outline-primary btn-sm">Xem chi tiết</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <% } %>
                            <% }) %>
                        <% } else { %>
                                <div class="alert alert-info">Bạn không có đơn hàng nào ở trạng thái <a style="text-decoration: underline;">đang giao hàng</a>.</div>
                        <% } %>                    
                    </div>
                    <div class="tab-pane fade" id="delivered" role="tabpanel" aria-labelledby="delivered-tab">
                        <% if (orderHistory && orderHistory.length> 0 && orderHistory.some(order => order.statusOrder === 'DELIVERED')) { %>
                            <% orderHistory.forEach((order) => { %>
                            <!-- Dữ liệu đơn hàng sẽ được render tại đây bằng EJS, mỗi đơn là một card -->
                             <% if (order.statusOrder === 'DELIVERED') { %>
                            <div class="row g-3">
                                <div class="col-12">
                                    <div class="card shadow-sm mb-3 border-success">
                                        <div
                                            class="card-body d-flex flex-column align-items-center justify-content-between">
                                            <div class="w-100">
                                                <div
                                                    class="d-flex flex-wrap align-items-center justify-content-between ">
                                                    <h5 class="card-title mb-1">Mã đơn hàng:
                                                        <span class="text-primary"><%= order.id %>
                                                        </span>
                                                    </h5>
                                                    <p class="mb-1">Trạng thái: <span
                                                            class="fw-bold text-success">
                                                            <% if (order.statusOrder === 'PENDING') { %>
                                                                Đang chờ xử lý 
                                                            <% } else if (order.statusOrder === 'PROCESSING') { %>
                                                                Đang xử lý
                                                            <% } else if (order.statusOrder === 'CONFIRMED') { %>
                                                                Đã xác nhận
                                                            <% } else if (order.statusOrder === 'PACKED') { %>
                                                                Đã đóng gói
                                                            <% } else if (order.statusOrder === 'SHIPPED') { %>
                                                                Đã giao cho đơn vị vận chuyển
                                                            <% } else if (order.statusOrder === 'DELIVERED') { %>
                                                                Đã giao thành công
                                                            <% } else if (order.statusOrder === 'CANCELLED') { %>
                                                                Đã hủy
                                                            <% } else if (order.statusOrder === 'RETURNED') { %>
                                                                Đã trả hàng
                                                            <% } else if (order.statusOrder === 'REFUNDED') { %>
                                                                Đã hoàn tiền
                                                            <% } else { %>
                                                                Không xác định
                                                            <% } %>
                                                        </span>
                                                    </p>
                                                </div>
                                                <br>
                                                <div class="table-responsive">
                                                    <table class="table text-center">
                                                        <thead>
                                                            <tr>
                                                                <th scope="col">Sản phẩm</th>
                                                                <th scope="col">Tên</th>
                                                                <th scope="col">Cấu hình</th>
                                                                <th scope="col">Giá</th>
                                                                <th scope="col">Số lượng</th>
                                                                <th scope="col">Tổng cộng</th>
                                                            </tr>
                                                        </thead>
                                                        <tbody>
                                                            <% if ( order.orderDetails && order.orderDetails.length > 0) { %>
                                                                <% order.orderDetails.forEach((item, index)=> { %>
                                                                    <tr class="detailCartId:<%= item.id %>">
                                                                        <th scope="row">
                                                                            <div
                                                                                class="d-flex align-items-center justify-content-center">
                                                                                <img src="<%= item.product.image ? '/images/products/' + item.product.image : '/client/img/default-product.png' %>"
                                                                                    class="img-fluid me-5 rounded-circle"
                                                                                    style="width: 80px; height: 80px; min-width: 50px; min-height: 50px;"
                                                                                    alt="image product <%= item.product.name %>">
                                                                            </div>
                                                                        </th>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-primary">
                                                                                <a href="/product/<%= item.product.id %>"
                                                                                    class="text-decoration-none fw-bold">
                                                                                    <%= item.product.name %>
                                                                                </a>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <% if (item.productVariant && item.productVariant.id) { %>
                                                                                <div class="d-flex justify-content-center align-items-center mt-4 w-100">
                                                                                    <span class="badge bg-info text-dark fs-5 px-4 py-2" style="font-size: 0.8rem !important;">
                                                                                        <%= item.productVariant.cpu %> | <%= item.productVariant.memory %> | <%= item.productVariant.color %>
                                                                                    </span>
                                                                                </div>
                                                                            <% } else { %>
                                                                                <span class="text-muted">Không có</span>
                                                                            <% } %>
                                                                        </td>
                                                                        <td>
                                                                            <% const variantPriceMore = (item.productVariant && item.productVariant.priceMore) ? item.productVariant.priceMore : 0; %>
                                                                            <% const unitPrice = (item.product && item.product.price ? item.product.price : 0) + variantPriceMore; %>
                                                                            <p class="mb-0 mt-4 text-dark fw-bold">
                                                                                <%= unitPrice.toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-warning fw-bold">
                                                                                <%= item.quantity %>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-danger fw-bold"
                                                                                data-OutPutCartDetail-id="<%= item.id %>">
                                                                                <%= (unitPrice * item.quantity).toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                    </tr>
                                                                    <% }); %>
                                                                        <% } else { %>
                                                                            <tr>
                                                                                <td colspan="6" class="text-center">Giỏ
                                                                                    hàng trống</td>
                                                                            </tr>
                                                                            <% } %>
                                                        </tbody>
                                                    </table>
                                                </div>
                                                <div class="d-flex flex-wrap align-items-center justify-content-end g-3"
                                                    style="gap: 1rem;">
                                                    <% const _pm = String(order.paymentMethod || '').toUpperCase(); const _ps = String(order.paymentStatus || '').toUpperCase(); const _isPaid = (_pm === 'PAYPAL' || _pm === 'CRYPTO') && (_ps === 'PAID' || _ps === 'PAYMENT_PAID'); %>
                                                    <p class="mb-1 mx-2">Phương thức thanh toán: <span
                                                            class="fw-bold text-success"><%= order.paymentMethod %>
                                                        </span>
                                                        <% if (_isPaid) { %>
                                                            <span class="badge bg-success ms-2">Đã thanh toán</span>
                                                        <% } %>
                                                    </p>
                                                    <p class="mb-1 mx-2">Số lượng sản phẩm: <span
                                                            class="fw-bold text-warning"><%= order.orderDetails.map(item => item.quantity).reduce((a, b) => a + b, 0) %>
                                                        </span></p>
                            <p class="mb-1 mx-4">Tổng số tiền: <span
                                class="fw-bold text-danger"><%= order.totalPrice.toLocaleString('vi-VN') %> VND</span></p>
                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof order.cryptoTotal !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                            <p class="mb-1 mx-4 text-muted small">Crypto total: ~ <%= order.cryptoTotal %> <%= cryptoActive.symbol || cryptoActive.code %></p>
                            <% } %>
                                                </div>
                                            </div>
                                            <% if (order.statusOrder === 'DELIVERED') { %>
                                                <% if (checkReviewed && checkReviewed.some(item => item.orderId === order.id)) { %>                                                        
                                                    <div class="alert alert-success w-100 text-center mb-0" role="alert">
                                                        Đơn hàng đã được giao thành công. Cảm ơn bạn đã mua sắm tại LaptopShop!
                                                    </div>
                                                <% } else { %>
                                                    <div class="alert alert-warning w-100 text-center mb-0" role="alert">
                                                        Đơn hàng này đã được giao thành công tới bạn. Vui lòng hoàn tất đánh giá để nhận ưu đãi từ LaptopShop!
                                                    </div>
                                                <% } %>
                                            <% } %>
                                            <div class="mt-3 d-flex justify-content-end w-100" style="gap: 0.5rem;">
                                                <% if (order.statusOrder === 'DELIVERED') { %>
                                                    <% if (checkReviewed && checkReviewed.some(item => item.orderId === order.id)) { %>
                                                        <a href="/review/<%= order.id %>" class="btn btn-outline-success btn-sm">Xem đánh giá</a>
                                                    <% } else { %>
                                                        <a href="/review/<%= order.id %>" class="btn btn-outline-success btn-sm">Đánh giá sản phẩm</a>
                                                    <% } %>
                                                <% } %>
                                                <a href="/order-history/<%= order.id %>" class="btn btn-outline-primary btn-sm">Xem chi tiết</a>                                                
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <% } %>
                            <% }) %>
                        <% } else { %>
                                <div class="alert alert-info">Bạn không có đơn hàng nào ở trạng thái <a style="text-decoration: underline;">đã giao thành công</a>.</div>
                        <% } %>
                    </div>
                    <div class="tab-pane fade" id="cancelled" role="tabpanel" aria-labelledby="cancelled-tab">
                        <% if (orderHistory && orderHistory.length> 0 && orderHistory.some(order => order.statusOrder === 'CANCELLED')) { %>
                            <% orderHistory.forEach((order) => { %>
                            <!-- Dữ liệu đơn hàng sẽ được render tại đây bằng EJS, mỗi đơn là một card -->
                             <% if (order.statusOrder === 'CANCELLED') { %>
                            <div class="row g-3">
                                <div class="col-12">
                                    <div class="card shadow-sm mb-3 border-danger">
                                        <div
                                            class="card-body d-flex flex-column align-items-center justify-content-between">
                                            <div class="w-100">
                                                <div
                                                    class="d-flex flex-wrap align-items-center justify-content-between ">
                                                    <h5 class="card-title mb-1">Mã đơn hàng:
                                                        <span class="text-primary"><%= order.id %>
                                                        </span>
                                                    </h5>
                                                    <p class="mb-1">Trạng thái: <span
                                                            class="fw-bold text-success">
                                                            <% if (order.statusOrder === 'PENDING') { %>
                                                                Đang chờ xử lý 
                                                            <% } else if (order.statusOrder === 'PROCESSING') { %>
                                                                Đang xử lý
                                                            <% } else if (order.statusOrder === 'CONFIRMED') { %>
                                                                Đã xác nhận
                                                            <% } else if (order.statusOrder === 'PACKED') { %>
                                                                Đã đóng gói
                                                            <% } else if (order.statusOrder === 'SHIPPED') { %>
                                                                Đã giao cho đơn vị vận chuyển
                                                            <% } else if (order.statusOrder === 'DELIVERED') { %>
                                                                Đã giao thành công
                                                            <% } else if (order.statusOrder === 'CANCELLED') { %>
                                                                Đã hủy
                                                            <% } else if (order.statusOrder === 'RETURNED') { %>
                                                                Đã trả hàng
                                                            <% } else if (order.statusOrder === 'REFUNDED') { %>
                                                                Đã hoàn tiền
                                                            <% } else { %>
                                                                Không xác định
                                                            <% } %>
                                                        </span>
                                                    </p>
                                                </div>
                                                <br>
                                                <div class="table-responsive">
                                                    <table class="table text-center">
                                                        <thead>
                                                            <tr>
                                                                <th scope="col">Sản phẩm</th>
                                                                <th scope="col">Tên</th>
                                                                <th scope="col">Cấu hình</th>
                                                                <th scope="col">Giá</th>
                                                                <th scope="col">Số lượng</th>
                                                                <th scope="col">Tổng cộng</th>
                                                            </tr>
                                                        </thead>
                                                        <tbody>
                                                            <% if ( order.orderDetails && order.orderDetails.length > 0) { %>
                                                                <% order.orderDetails.forEach((item, index)=> { %>
                                                                    <tr class="detailCartId:<%= item.id %>">
                                                                        <th scope="row">
                                                                            <div
                                                                                class="d-flex align-items-center justify-content-center">
                                                                                <img src="<%= item.product.image ? '/images/products/' + item.product.image : '/client/img/default-product.png' %>"
                                                                                    class="img-fluid me-5 rounded-circle"
                                                                                    style="width: 80px; height: 80px; min-width: 50px; min-height: 50px;"
                                                                                    alt="image product <%= item.product.name %>">
                                                                            </div>
                                                                        </th>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-primary">
                                                                                <a href="/product/<%= item.product.id %>"
                                                                                    class="text-decoration-none fw-bold">
                                                                                    <%= item.product.name %>
                                                                                </a>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <% if (item.productVariant && item.productVariant.id) { %>
                                                                                <div class="d-flex justify-content-center align-items-center mt-4 w-100">
                                                                                    <span class="badge bg-info text-dark fs-5 px-4 py-2" style="font-size: 0.8rem !important;">
                                                                                        <%= item.productVariant.cpu %> | <%= item.productVariant.memory %> | <%= item.productVariant.color %>
                                                                                    </span>
                                                                                </div>
                                                                            <% } else { %>
                                                                                <span class="text-muted">Không có</span>
                                                                            <% } %>
                                                                        </td>
                                                                        <td>
                                                                            <% const variantPriceMore = (item.productVariant && item.productVariant.priceMore) ? item.productVariant.priceMore : 0; %>
                                                                            <% const unitPrice = (item.product && item.product.price ? item.product.price : 0) + variantPriceMore; %>
                                                                            <p class="mb-0 mt-4 text-dark fw-bold">
                                                                                <%= unitPrice.toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-warning fw-bold">
                                                                                <%= item.quantity %>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-danger fw-bold"
                                                                                data-OutPutCartDetail-id="<%= item.id %>">
                                                                                <%= (unitPrice * item.quantity).toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                    </tr>
                                                                    <% }); %>
                                                                        <% } else { %>
                                                                            <tr>
                                                                                <td colspan="6" class="text-center">Giỏ
                                                                                    hàng trống</td>
                                                                            </tr>
                                                                            <% } %>
                                                        </tbody>
                                                    </table>
                                                </div>
                                                <div class="d-flex flex-wrap align-items-center justify-content-end g-3"
                                                    style="gap: 1rem;">
                                                    <% const _pm = String(order.paymentMethod || '').toUpperCase(); const _ps = String(order.paymentStatus || '').toUpperCase(); const _isPaid = (_pm === 'PAYPAL' || _pm === 'CRYPTO') && (_ps === 'PAID' || _ps === 'PAYMENT_PAID'); %>
                                                    <p class="mb-1 mx-2">Phương thức thanh toán: <span
                                                            class="fw-bold text-success"><%= order.paymentMethod %>
                                                        </span>
                                                        <% if (_isPaid) { %>
                                                            <span class="badge bg-success ms-2">Đã thanh toán</span>
                                                        <% } %>
                                                    </p>
                                                    <p class="mb-1 mx-2">Số lượng sản phẩm: <span
                                                            class="fw-bold text-warning"><%= order.orderDetails.map(item => item.quantity).reduce((a, b) => a + b, 0) %>
                                                        </span></p>
                            <p class="mb-1 mx-4">Tổng số tiền: <span
                                class="fw-bold text-danger"><%= order.totalPrice.toLocaleString('vi-VN') %> VND</span></p>
                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof order.cryptoTotal !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                            <p class="mb-1 mx-4 text-muted small">Crypto total: ~ <%= order.cryptoTotal %> <%= cryptoActive.symbol || cryptoActive.code %></p>
                            <% } %>
                                                </div>
                                            </div>
                                            <div class="mt-3 d-flex justify-content-end w-100">
                                                <a href="/order-history/<%= order.id %>" class="btn btn-outline-primary btn-sm">Xem chi tiết</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <% } %>
                            <% }) %>
                        <% } else { %>
                                <div class="alert alert-danger">Bạn không có đơn hàng nào ở trạng thái <a style="text-decoration: underline;">đã bị hủy</a>.</div>
                        <% } %>
                    </div>
                    <div class="tab-pane fade" id="returned" role="tabpanel" aria-labelledby="returned-tab">
                        <% if (orderHistory && orderHistory.length> 0 && orderHistory.some(order => order.statusOrder === 'RETURNED')) { %>
                            <% orderHistory.forEach((order) => { %>
                            <!-- Dữ liệu đơn hàng sẽ được render tại đây bằng EJS, mỗi đơn là một card -->
                             <% if (order.statusOrder === 'RETURNED') { %>
                            <div class="row g-3">
                                <div class="col-12">
                                    <div class="card shadow-sm mb-3 border-warning">
                                        <div
                                            class="card-body d-flex flex-column align-items-center justify-content-between">
                                            <div class="w-100">
                                                <div
                                                    class="d-flex flex-wrap align-items-center justify-content-between ">
                                                    <h5 class="card-title mb-1">Mã đơn hàng:
                                                        <span class="text-primary"><%= order.id %>
                                                        </span>
                                                    </h5>
                                                    <p class="mb-1">Trạng thái: <span
                                                            class="fw-bold text-success">
                                                            <% if (order.statusOrder === 'PENDING') { %>
                                                                Đang chờ xử lý 
                                                            <% } else if (order.statusOrder === 'PROCESSING') { %>
                                                                Đang xử lý
                                                            <% } else if (order.statusOrder === 'CONFIRMED') { %>
                                                                Đã xác nhận
                                                            <% } else if (order.statusOrder === 'PACKED') { %>
                                                                Đã đóng gói
                                                            <% } else if (order.statusOrder === 'SHIPPED') { %>
                                                                Đã giao cho đơn vị vận chuyển
                                                            <% } else if (order.statusOrder === 'DELIVERED') { %>
                                                                Đã giao thành công
                                                            <% } else if (order.statusOrder === 'CANCELLED') { %>
                                                                Đã hủy
                                                            <% } else if (order.statusOrder === 'RETURNED') { %>
                                                                Đã trả hàng
                                                            <% } else if (order.statusOrder === 'REFUNDED') { %>
                                                                Đã hoàn tiền
                                                            <% } else { %>
                                                                Không xác định
                                                            <% } %>
                                                        </span>
                                                    </p>
                                                </div>
                                                <br>
                                                <div class="table-responsive">
                                                    <table class="table text-center">
                                                        <thead>
                                                            <tr>
                                                                <th scope="col">Sản phẩm</th>
                                                                <th scope="col">Tên</th>
                                                                <th scope="col">Cấu hình</th>
                                                                <th scope="col">Giá</th>
                                                                <th scope="col">Số lượng</th>
                                                                <th scope="col">Tổng cộng</th>
                                                            </tr>
                                                        </thead>
                                                        <tbody>
                                                            <% if ( order.orderDetails && order.orderDetails.length > 0) { %>
                                                                <% order.orderDetails.forEach((item, index)=> { %>
                                                                    <tr class="detailCartId:<%= item.id %>">
                                                                        <th scope="row">
                                                                            <div
                                                                                class="d-flex align-items-center justify-content-center">
                                                                                <img src="<%= item.product.image ? '/images/products/' + item.product.image : '/client/img/default-product.png' %>"
                                                                                    class="img-fluid me-5 rounded-circle"
                                                                                    style="width: 80px; height: 80px; min-width: 50px; min-height: 50px;"
                                                                                    alt="image product <%= item.product.name %>">
                                                                            </div>
                                                                        </th>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-primary">
                                                                                <a href="/product/<%= item.product.id %>"
                                                                                    class="text-decoration-none fw-bold">
                                                                                    <%= item.product.name %>
                                                                                </a>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <% if (item.productVariant && item.productVariant.id) { %>
                                                                                <div class="d-flex justify-content-center align-items-center mt-4 w-100">
                                                                                    <span class="badge bg-info text-dark fs-5 px-4 py-2" style="font-size: 0.8rem !important;">
                                                                                        <%= item.productVariant.cpu %> | <%= item.productVariant.memory %> | <%= item.productVariant.color %>
                                                                                    </span>
                                                                                </div>
                                                                            <% } else { %>
                                                                                <span class="text-muted">Không có</span>
                                                                            <% } %>
                                                                        </td>
                                                                        <td>
                                                                            <% const variantPriceMore = (item.productVariant && item.productVariant.priceMore) ? item.productVariant.priceMore : 0; %>
                                                                            <% const unitPrice = (item.product && item.product.price ? item.product.price : 0) + variantPriceMore; %>
                                                                            <p class="mb-0 mt-4 text-dark fw-bold">
                                                                                <%= unitPrice.toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-warning fw-bold">
                                                                                <%= item.quantity %>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-danger fw-bold"
                                                                                data-OutPutCartDetail-id="<%= item.id %>">
                                                                                <%= (unitPrice * item.quantity).toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                    </tr>
                                                                    <% }); %>
                                                                        <% } else { %>
                                                                            <tr>
                                                                                <td colspan="6" class="text-center">Giỏ
                                                                                    hàng trống</td>
                                                                            </tr>
                                                                            <% } %>
                                                        </tbody>
                                                    </table>
                                                </div>
                                                <div class="d-flex flex-wrap align-items-center justify-content-end g-3"
                                                    style="gap: 1rem;">
                                                    <% const _pm = String(order.paymentMethod || '').toUpperCase(); const _ps = String(order.paymentStatus || '').toUpperCase(); const _isPaid = (_pm === 'PAYPAL' || _pm === 'CRYPTO') && (_ps === 'PAID' || _ps === 'PAYMENT_PAID'); %>
                                                    <p class="mb-1 mx-2">Phương thức thanh toán: <span
                                                            class="fw-bold text-success"><%= order.paymentMethod %>
                                                        </span>
                                                        <% if (_isPaid) { %>
                                                            <span class="badge bg-success ms-2">Đã thanh toán</span>
                                                        <% } %>
                                                    </p>
                                                    <p class="mb-1 mx-2">Số lượng sản phẩm: <span
                                                            class="fw-bold text-warning"><%= order.orderDetails.map(item => item.quantity).reduce((a, b) => a + b, 0) %>
                                                        </span></p>
                            <p class="mb-1 mx-4">Tổng số tiền: <span
                                class="fw-bold text-danger"><%= order.totalPrice.toLocaleString('vi-VN') %> VND</span></p>
                            <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof order.cryptoTotal !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                            <p class="mb-1 mx-4 text-muted small">Crypto total: ~ <%= order.cryptoTotal %> <%= cryptoActive.symbol || cryptoActive.code %></p>
                            <% } %>
                                                </div>
                                            </div>
                                            <div class="mt-3 d-flex justify-content-end w-100">
                                                <a href="/order-history/<%= order.id %>" class="btn btn-outline-primary btn-sm">Xem chi tiết</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <% } %>
                            <% }) %>
                        <% } else { %>
                                <div class="alert alert-info">Bạn không có đơn hàng nào ở trạng thái <a style="text-decoration: underline;">trả hàng</a>.</div>
                        <% } %>
                    </div>
                    <div class="tab-pane fade" id="refunded" role="tabpanel" aria-labelledby="refunded-tab">
                        <% if (orderHistory && orderHistory.length> 0 && orderHistory.some(order => order.statusOrder === 'REFUNDED')) { %>
                            <% orderHistory.forEach((order) => { %>
                            <!-- Dữ liệu đơn hàng sẽ được render tại đây bằng EJS, mỗi đơn là một card -->
                             <% if (order.statusOrder === 'REFUNDED') { %>
                            <div class="row g-3">
                                <div class="col-12">
                                    <div class="card shadow-sm mb-3 border-warning">
                                        <div
                                            class="card-body d-flex flex-column align-items-center justify-content-between">
                                            <div class="w-100">
                                                <div
                                                    class="d-flex flex-wrap align-items-center justify-content-between ">
                                                    <h5 class="card-title mb-1">Mã đơn hàng:
                                                        <span class="text-primary"><%= order.id %>
                                                        </span>
                                                    </h5>
                                                    <p class="mb-1">Trạng thái: <span
                                                            class="fw-bold text-success">
                                                            <% if (order.statusOrder === 'PENDING') { %>
                                                                Đang chờ xử lý 
                                                            <% } else if (order.statusOrder === 'PROCESSING') { %>
                                                                Đang xử lý
                                                            <% } else if (order.statusOrder === 'CONFIRMED') { %>
                                                                Đã xác nhận
                                                            <% } else if (order.statusOrder === 'PACKED') { %>
                                                                Đã đóng gói
                                                            <% } else if (order.statusOrder === 'SHIPPED') { %>
                                                                Đã giao cho đơn vị vận chuyển
                                                            <% } else if (order.statusOrder === 'DELIVERED') { %>
                                                                Đã giao thành công
                                                            <% } else if (order.statusOrder === 'CANCELLED') { %>
                                                                Đã hủy
                                                            <% } else if (order.statusOrder === 'RETURNED') { %>
                                                                Đã trả hàng
                                                            <% } else if (order.statusOrder === 'REFUNDED') { %>
                                                                Đã hoàn tiền
                                                            <% } else { %>
                                                                Không xác định
                                                            <% } %>
                                                        </span>
                                                    </p>
                                                </div>
                                                <br>
                                                <div class="table-responsive">
                                                    <table class="table text-center">
                                                        <thead>
                                                            <tr>
                                                                <th scope="col">Sản phẩm</th>
                                                                <th scope="col">Tên</th>
                                                                <th scope="col">Cấu hình</th>
                                                                <th scope="col">Giá</th>
                                                                <th scope="col">Số lượng</th>
                                                                <th scope="col">Tổng cộng</th>
                                                            </tr>
                                                        </thead>
                                                        <tbody>
                                                            <% if ( order.orderDetails && order.orderDetails.length > 0) { %>
                                                                <% order.orderDetails.forEach((item, index)=> { %>
                                                                    <tr class="detailCartId:<%= item.id %>">
                                                                        <th scope="row">
                                                                            <div
                                                                                class="d-flex align-items-center justify-content-center">
                                                                                <img src="<%= item.product.image ? '/images/products/' + item.product.image : '/client/img/default-product.png' %>"
                                                                                    class="img-fluid me-5 rounded-circle"
                                                                                    style="width: 80px; height: 80px; min-width: 50px; min-height: 50px;"
                                                                                    alt="image product <%= item.product.name %>">
                                                                            </div>
                                                                        </th>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-primary">
                                                                                <a href="/product/<%= item.product.id %>"
                                                                                    class="text-decoration-none fw-bold">
                                                                                    <%= item.product.name %>
                                                                                </a>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <% if (item.productVariant && item.productVariant.id) { %>
                                                                                <div class="d-flex justify-content-center align-items-center mt-4 w-100">
                                                                                    <span class="badge bg-info text-dark fs-5 px-4 py-2" style="font-size: 0.8rem !important;">
                                                                                        <%= item.productVariant.cpu %> | <%= item.productVariant.memory %> | <%= item.productVariant.color %>
                                                                                    </span>
                                                                                </div>
                                                                            <% } else { %>
                                                                                <span class="text-muted">Không có</span>
                                                                            <% } %>
                                                                        </td>
                                                                        <td>
                                                                            <% const variantPriceMore = (item.productVariant && item.productVariant.priceMore) ? item.productVariant.priceMore : 0; %>
                                                                            <% const unitPrice = (item.product && item.product.price ? item.product.price : 0) + variantPriceMore; %>
                                                                            <p class="mb-0 mt-4 text-dark fw-bold">
                                                                                <%= unitPrice.toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-warning fw-bold">
                                                                                <%= item.quantity %>
                                                                            </p>
                                                                        </td>
                                                                        <td>
                                                                            <p class="mb-0 mt-4 text-danger fw-bold"
                                                                                data-OutPutCartDetail-id="<%= item.id %>">
                                                                                <%= (unitPrice * item.quantity).toLocaleString('vi-VN') %> VND
                                                                            </p>
                                                                        </td>
                                                                    </tr>
                                                                    <% }); %>
                                                                        <% } else { %>
                                                                            <tr>
                                                                                <td colspan="6" class="text-center">Giỏ
                                                                                    hàng trống</td>
                                                                            </tr>
                                                                            <% } %>
                                                        </tbody>
                                                    </table>
                                                </div>
                                                <div class="d-flex flex-wrap align-items-center justify-content-end g-3"
                                                    style="gap: 1rem;">
                                                    <% const _pm = String(order.paymentMethod || '').toUpperCase(); const _ps = String(order.paymentStatus || '').toUpperCase(); const _isPaid = (_pm === 'PAYPAL' || _pm === 'CRYPTO') && (_ps === 'PAID' || _ps === 'PAYMENT_PAID'); %>
                                                    <p class="mb-1 mx-2">Phương thức thanh toán: <span
                                                            class="fw-bold text-success"><%= order.paymentMethod %>
                                                        </span>
                                                        <% if (_isPaid) { %>
                                                            <span class="badge bg-success ms-2">Đã thanh toán</span>
                                                        <% } %>
                                                    </p>
                                                    <p class="mb-1 mx-2">Số lượng sản phẩm: <span
                                                            class="fw-bold text-warning"><%= order.orderDetails.map(item => item.quantity).reduce((a, b) => a + b, 0) %>
                                                        </span></p>
                                                        <p class="mb-1 mx-4">Tổng số tiền: <span
                                                            class="fw-bold text-danger"><%= order.totalPrice.toLocaleString('vi-VN') %> VND</span></p>
                                                        <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof order.cryptoTotal !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                                                            <p class="mb-1 mx-4 text-muted small">Crypto total: ~ <%= order.cryptoTotal %> <%= cryptoActive.symbol || cryptoActive.code %></p>
                                                        <% } %>
                                                        <% if (String(order.paymentMethod || '').toUpperCase() === 'CRYPTO' && typeof order.cryptoTotal !== 'undefined' && typeof cryptoActive !== 'undefined') { %>
                                                        <p class="mb-1 mx-4 text-muted small">Crypto total: ~ <%= order.cryptoTotal %> <%= cryptoActive.symbol || cryptoActive.code %></p>
                                                        <% } %>
                                                </div>
                                            </div>
                                            <div class="mt-3 d-flex justify-content-end w-100">
                                                <a href="/order-history/<%= order.id %>" class="btn btn-outline-primary btn-sm">Xem chi tiết</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <% } %>
                            <% }) %>
                        <% } else { %>
                                <div class="alert alert-info">Bạn không có đơn hàng nào ở trạng thái <a style="text-decoration: underline;">Đã hoàn tiền</a>.</div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
        <!-- Single Product End -->


        <!-- Footer Start -->
        <%- include('../layout/footer.ejs') -%>
            <!-- Footer End -->




            <!-- Back to Top -->
            <a href="#" class="btn btn-primary border-3 border-primary rounded-circle back-to-top"><i
                    class="fa fa-arrow-up"></i></a>


            <!-- JavaScript Libraries -->
            <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.4/jquery.min.js"></script>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
            <script src="/client/lib/easing/easing.min.js"></script>
            <script src="/client/lib/waypoints/waypoints.min.js"></script>
            <script src="/client/lib/lightbox/js/lightbox.min.js"></script>
            <script src="/client/lib/owlcarousel/owl.carousel.min.js"></script>

            <!-- Template Javascript -->
            <script src="/client/js/main.js"></script>
            <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-toast-plugin/1.3.2/jquery.toast.min.js" integrity="sha512-zlWWyZq71UMApAjih4WkaRpikgY9Bz1oXIW5G0fED4vk14JjGlQ1UmkGM392jEULP8jbNMiwLWdM8Z87Hu88Fw==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>

</body>

</html>