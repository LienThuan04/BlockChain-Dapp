<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="LianHarman - Dự án laptopshop" />
    <meta name="author" content="LianHarman" />
    <title>Dashboard - LianHarman</title>
    <link rel="icon" type="image/svg+xml" href="/im/Logo-Admin.svg">
    <link href="/admin/css/styles.css" rel="stylesheet" />
    <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>
</head>

<body class="sb-nav-fixed">
    <!-- header -->
    <%- include('../layout/header.ejs') -%>

    <!-- main content -->
    <div id="layoutSidenav">
        <!-- sidenav -->
        <%- include('../layout/sidenav.ejs') -%>
        <div id="layoutSidenav_content">
            <main>
                <div class="container-fluid px-4">
                    <h1 class="mt-4 fw-bold text-gradient" style="background: linear-gradient(90deg,#007bff,#00c6ff,#43e97b 60%,#38f9d7);-webkit-background-clip: text;-webkit-text-fill-color: transparent;">Admin Dashboard</h1>
                    <ol class="breadcrumb mb-4 bg-light rounded shadow-sm px-3 py-2" style="cursor: pointer;">
                        <li class="breadcrumb-item active"><i class="fas fa-home me-1"></i> Dashboard</li>
                    </ol>
                    <div class="row g-4 mb-4">
                        <div class="col-xl-3 col-md-6">
                            <div class="card shadow border-0 mb-4" style="background: linear-gradient(135deg,#007bff 60%,#00c6ff);">
                                <div class="card-body d-flex align-items-center">
                                    <i class="fas fa-users fa-3x me-3 opacity-75"></i>
                                    <div>
                                        <h5 class="mb-1">Users</h5>
                                        <% if (dashboard.users) { %>
                                            <h3 class="fw-bold mb-0"><%= dashboard.users %></h3>
                                        <% } else { %>
                                            <h3 class="fw-bold mb-0">0</h3>
                                        <% } %>
                                    </div>
                                </div>
                                <div class="card-footer d-flex align-items-center justify-content-between bg-transparent border-0">
                                    <a class="small text-white stretched-link" href="/admin/user">View Details</a>
                                    <div class="small text-white"><i class="fas fa-angle-right"></i></div>
                                </div>
                            </div>
                        </div>
                        <div class="col-xl-3 col-md-6">
                            <div class="card shadow border-0 mb-4" style="background: linear-gradient(135deg,#ffb347 60%,#ffcc33);">
                                <div class="card-body d-flex align-items-center">
                                    <i class="fas fa-box fa-3x me-3 opacity-75"></i>
                                    <div>
                                        <h5 class="mb-1">Products</h5>
                                        <% if (dashboard.products) { %>
                                            <h3 class="fw-bold mb-0"><%= dashboard.products %></h3>
                                        <% } else { %>
                                            <h3 class="fw-bold mb-0">0</h3>
                                        <% } %>
                                    </div>
                                </div>
                                <div class="card-footer d-flex align-items-center justify-content-between bg-transparent border-0">
                                    <a class="small text-white stretched-link" href="/admin/product">View Details</a>
                                    <div class="small text-white"><i class="fas fa-angle-right"></i></div>
                                </div>
                            </div>
                        </div>
                        <div class="col-xl-3 col-md-6">
                            <div class="card shadow border-0 mb-4" style="background: linear-gradient(135deg,#43e97b 60%,#38f9d7);">
                                <div class="card-body d-flex align-items-center">
                                    <i class="fas fa-shopping-cart fa-3x me-3 opacity-75"></i>
                                    <div>
                                        <h5 class="mb-1">Orders</h5>
                                        <% if (dashboard.orders) { %>
                                            <h3 class="fw-bold mb-0"><%= dashboard.orders %></h3>
                                        <% } else { %>
                                            <h3 class="fw-bold mb-0">0</h3>
                                        <% } %>
                                    </div>
                                </div>
                                <div class="card-footer d-flex align-items-center justify-content-between bg-transparent border-0">
                                    <a class="small text-white stretched-link" href="/admin/order">View Details</a>
                                    <div class="small text-white"><i class="fas fa-angle-right"></i></div>
                                </div>
                            </div>
                        </div>
                        <div class="col-xl-3 col-md-6">
                            <div class="card shadow border-0 mb-4" style="background: linear-gradient(135deg,#8f94fb 60%,#4e54c8);">
                                <div class="card-body d-flex align-items-center">
                                    <i class="fas fa-industry fa-3x me-3 opacity-75"></i>
                                    <div>
                                        <h5 class="mb-1">Factory</h5>
                                        <% if (dashboard.factory) { %>
                                            <h3 class="fw-bold mb-0"><%= dashboard.factory %></h3>
                                        <% } else { %>
                                            <h3 class="fw-bold mb-0">0</h3>
                                        <% } %>
                                    </div>
                                </div>
                                <div class="card-footer d-flex align-items-center justify-content-between bg-transparent border-0">
                                    <a class="small text-white stretched-link" href="/admin/factory">View Details</a>
                                    <div class="small text-white"><i class="fas fa-angle-right"></i></div>
                                </div>
                            </div>
                        </div>
                        <div class="col-xl-3 col-md-6">
                            <div class="card shadow border-0 mb-4" style="background: linear-gradient(135deg,#f7971e 60%,#ffd200);">
                                <div class="card-body d-flex align-items-center">
                                    <i class="fas fa-bullseye fa-3x me-3 opacity-75"></i>
                                    <div>
                                        <h5 class="mb-1">Target</h5>
                                        <% if (dashboard.target) { %>
                                            <h3 class="fw-bold mb-0"><%= dashboard.target %></h3>
                                        <% } else { %>
                                            <h3 class="fw-bold mb-0">0</h3>
                                        <% } %>
                                    </div>
                                </div>
                                <div class="card-footer d-flex align-items-center justify-content-between bg-transparent border-0">
                                    <a class="small text-white stretched-link" href="/admin/target">View Details</a>
                                    <div class="small text-white"><i class="fas fa-angle-right"></i></div>
                                </div>
                            </div>
                        </div>
                        <div class="col-xl-3 col-md-6">
                            <div class="card shadow border-0 mb-4" style="background: linear-gradient(135deg,#dcdc04 60%,rgb(239, 255, 18));">
                                <div class="card-body d-flex align-items-center">
                                    <i class="fas fa-star fa-3x me-3 opacity-75"></i>
                                    <div>
                                        <h5 class="mb-1">Reviews</h5>
                                        <% if (dashboard.reviews) { %>
                                            <h3 class="fw-bold mb-0"><%= dashboard.reviews %></h3>
                                        <% } else { %>
                                            <h3 class="fw-bold mb-0">0</h3>
                                        <% } %>
                                    </div>
                                </div>
                                <div class="card-footer d-flex align-items-center justify-content-between bg-transparent border-0">
                                    <a class="small text-white stretched-link" href="/admin/review">View Details</a>
                                    <div class="small text-white"><i class="fas fa-angle-right"></i></div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-12 col-xl-12">
                            <input type="hidden" id="revenueDataInput" value='<%= JSON.stringify(revenue) %>' />
                            <div class="card mb-4">
                                <div class="card-header">
                                    <i class="fas fa-chart-area me-1"></i>
                                    Revenue of successfully delivered products by month
                                </div>
                                <div class="card-body"><canvas id="myAreaChart" width="100%" height="30"></canvas></div>
                                <div class="card-footer small text-muted">Updated <%= new Date().toLocaleDateString('vi-VN') %> at <%= new Date().toLocaleTimeString('vi-VN', { hour: '2-digit', minute: '2-digit', hour12: false }) %></div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-lg-6">
                                <div class="card mb-4">
                                    <div class="card-header">
                                        <i class="fas fa-chart-bar me-1"></i>
                                        Bar Chart of Top Contributors
                                    </div>
                                    <div class="card-body"><canvas id="myBarChart" width="100%" height="50"></canvas></div>
                                    <div class="card-footer small text-muted">Updated <%= new Date().toLocaleDateString('vi-VN') %> at <%= new Date().toLocaleTimeString('vi-VN', { hour: '2-digit', minute: '2-digit', hour12: false }) %></div>
                                </div>
                            </div>
                            <div class="col-lg-6">
                                <div class="card mb-4">
                                    <div class="card-header">
                                        <i class="fas fa-chart-pie me-1"></i>
                                        Pie Chart of Best Selling Products
                                    </div>
                                    <div class="card-body"><canvas id="myPieChart" width="100%" height="50"></canvas></div>
                                    <div class="card-footer small text-muted">Updated <%= new Date().toLocaleDateString('vi-VN') %> at <%= new Date().toLocaleTimeString('vi-VN', { hour: '2-digit', minute: '2-digit', hour12: false }) %></div>
                                </div>
                            </div>
                        </div>
                        <div class="col-12">
                            <div class="card shadow border-0 mb-4">
                                <div class="card-header bg-white fw-bold"><i class="fas fa-info-circle me-2 text-primary"></i>Welcome to the Admin Dashboard</div>
                                <div class="card-body">
                                    <p class="lead">Quickly manage users, products, orders, and view analytics. Use the navigation menu to access each section. This dashboard is designed for a modern, clean, and responsive experience.</p>
                                    <ul class="list-unstyled mb-0">
                                        <li><i class="fas fa-check-circle text-success me-2"></i> Beautiful gradient cards with icons</li>
                                        <li><i class="fas fa-check-circle text-success me-2"></i> Responsive layout for all devices</li>
                                        <li><i class="fas fa-check-circle text-success me-2"></i> Easy navigation to all admin features</li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </main>
            <!-- footer -->
            <%- include('../layout/footer.ejs') -%>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="/admin/js/scripts.js"></script>
    <script src="/admin/js/revenueForAdmin.js"></script>

</body>

</html>